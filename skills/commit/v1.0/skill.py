"""Commit skill — git commit generated code."""
from typing import Dict, Any

async def commit(message: str, files: list, repo_path: str) -> Dict[str, Any]:
    """
    Commit files to git.

    Returns:
        {"commit_sha": str}
    """
    # TODO: Implement git commit
    return {"commit_sha": "abc123"}
